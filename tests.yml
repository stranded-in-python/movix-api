version: "3.8"

name: "movix_tests"

services:
  api:
    image: ghcr.io/stranded-in-python/movix-api:dev
    stdin_open: true
    command: "pytest /app/tests/test_services/ && pytest /app/tests/functional/ && pytest /app/tests/test_db"
    volumes:
      - ./src:/app:z
    entrypoint: ""
    ports:
      - "8001:8000"
